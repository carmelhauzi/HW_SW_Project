[project]
name = "pyperformance_bm_nbody_optimized"
requires-python = ">=3.8"
dependencies = ["pyperf"]
urls = {repository = "https://github.com/python/pyperformance"}
version = "1.0.0"

[tool.pyperformance]
name = "nbody"
tags = "math"
